---
- name: systemd-container role
  tags: [role-systemd-container, role-systemd-nspawn]
  block:

  # install
  - ansible.builtin.import_tasks: systemd-container-install.yml
 
  # nspawn containers
  - ansible.builtin.import_tasks: systemd-nspawn-containers.yml

  # nspawn_service_overrides
  - ansible.builtin.import_tasks: systemd-nspawn-service-overrides.yml